<LinkTo @route="cart" class="cart-link">
  <i class="fa fa-shopping-cart"></i>
  {{#if this.hasShopped}}
    <span class="d-inline-flex justify-content-center align-items-center cart-count">{{this.shopped}}</span>
  {{/if}}
</LinkTo>
<main class="container mt-5">
  {{yield}}
</main>
